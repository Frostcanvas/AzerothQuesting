name: Apply Beta 10 hotfix

on:
  push:
    paths:
      - .github/workflows/apply-beta10-network-fix.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Validate runner
        run: echo ready
